# 시크릿은 어떤 경우에도 이미지에 들어가면 안 된다.
**/application-secret.yml
**/*.pem
**/*.key
.env
.env.*

# 빌드 산출물·로컬 상태 (이미지 안에서 새로 빌드한다)
build/
*/build/
.gradle/
.git/
.github/

# IDE
.idea/
*.iml
*.ipr
*.iws

# 하네스·문서 — 런타임/빌드에 불필요
.ai-context/
.claude/
.codex/
AGENTS.md
CLAUDE.md
api-contract/
docs/
tools/

# 컨테이너 정의 자체
Dockerfile
.dockerignore
docker-compose.yml

# 모니터링 스택 — 앱과 별개로 뜨므로 이미지에 들어갈 이유가 없다
monitoring/
