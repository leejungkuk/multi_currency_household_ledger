apiVersion: 1

policies:
  - orgId: 1
    receiver: ntfy
    group_by: [alertname]
    # 첫 알림은 빨리, 반복은 느리게. 백업 실패처럼 사람이 고칠 때까지 유지되는 상태를
    # 5분마다 다시 울리면 결국 무음 처리된다.
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 12h
